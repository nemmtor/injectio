import { Injectio } from '@injectio/react';
import { describe, expect, it } from '@effect/vitest';

describe('Injectio', () => {
  it('is defined', () => {
    expect(Injectio).toBeDefined();
  });
});
